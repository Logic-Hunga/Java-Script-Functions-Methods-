// defining the function to call
function comparisonFunction(phoneNumber) {
    if (phoneNumber) { // evaluates true only
        return "Its true phone number exists";
    }
    return "Its false phone number aint exist";
}