// defining the void function
function productFunction(number1, number2) {
    var product = number1 * number2;
    console.log("The of product of " + number1 + "+ and " + number2 + " is: " + product);
}
productFunction(12, 10);


// defining the void function
function summationFunction(number1, number2) {
    var summation = number1 + number2;
    console.log("The sum of number " + number1 + " " + number2 + " is " + summation);
}
summationFunction(12, 10);

// defining the void function
function quotientFunction(number1, number2) {
    var quotient = number1 / number2;
    console.log("The quotient of  " + number1 + " and " + number2 +" is " + quotient);
}
quotientFunction(12, 10);